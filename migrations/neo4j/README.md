# migrations/neo4j

Cypher migrations for the block adjacency / transition graph. Empty in
PR 1. First real migrations land in PR 6 and should define constraints,
indexes, and the node/edge shape described in
`docs/architecture.md#constraint-graph`.
